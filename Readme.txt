Для старта нужна только база "test", таблица "user" создается автоматом (параметр "hibernate.hbm2ddl.auto" установлен в "update"),
на форме есть кнопка генерации "Generate Users", создает по 30 рандомных записей в таблицу.

